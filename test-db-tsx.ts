import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const c = await prisma.company.create({ data: { name: 'AXA Coach Services Test', email: 'admin.test-final@axa.com', phone: '+265 888 654 321', address: 'Area 47, Lilongwe', description: 'Brief description of the company test', status: 'pending', setupCompleted: false, createdAt: new Date(), updatedAt: new Date() } }); console.log(JSON.stringify(c, null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
